import styled from "styled-components";

export const TestimonialsCardStyle = styled.section`
	& > .TestimonialsCardStyle {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 2rem;
		& > .subsection {
			display: flex;
			flex-direction: column;
			gap: 1rem;
		}
	}
`;
